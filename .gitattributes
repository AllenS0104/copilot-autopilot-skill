# Shell scripts must use LF so they run correctly on Ubuntu/Linux and macOS,
# even when the repo is cloned or edited on Windows.
*.sh    text eol=lf

# PowerShell assets keep CRLF for Windows.
*.ps1   text eol=crlf
